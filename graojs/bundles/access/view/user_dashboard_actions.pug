.form-group.pull-right
  button.btn.btn-default(ng-click='queryUser(); countUser(); share.window("listUser");')=__('List')
  | 
  button.btn.btn-success(ng-click='clearUser(); share.window("createOrUpdateUser");')=__('New')